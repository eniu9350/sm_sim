#include "sim_env.h"


#include "input_generator.h"
#include <math.h>
#include <stdlib.h>

sim_env se;

int main()
{
	sim_env_init(&se);

	//ev_loop_loop(se.el);


	/*
	int n;

	int* d;
	d = (int*)malloc(2500*sizeof(int));
	generate_duration(2500, d);
	*/


}
